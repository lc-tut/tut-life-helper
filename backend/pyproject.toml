[project]
name = "tut-life-helper-backend"
version = "0.1.0"
description = "Backend for TUT Life Helper"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.29.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
